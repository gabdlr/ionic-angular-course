import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.angular',
  appName: 'ionic-angular-course',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
